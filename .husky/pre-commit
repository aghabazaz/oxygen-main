# #!/usr/bin/env sh
# . "$(dirname -- "$0")/_/husky.sh"

# if [ -x "$(command -v pnpm)" ]; then
#     pnpm lint-staged
#     exit
# fi
# if [ -x "$(command -v npm)" ]; then
#     npm run lint-staged
#     exit
# fi
# echo $"\n  pnpm or npm \b"
# exit 1

